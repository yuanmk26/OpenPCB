# 2026-03-22_no-code-change-git-remove-added-directory

## 变更前问题
- 用户咨询 Git 操作中如何删除已经 `add` 进去的目录。
- 当前任务仅需提供操作说明，不涉及仓库源码或配置调整。
- 需要按仓库规范补充一次 `no-code-change` 变更记录。

## 变更内容
- 新增 `no-code-change` 变更记录，记录本次 Git 使用说明类支持。
- 未修改任何源码、配置或工作流文件。
- 输出区分“仅取消暂存”和“从版本库移除目录”两种常见场景。

## 影响范围
- 影响范围仅限变更记录文档。
- 新增文件：`docs/changes/2026-03-22/2026-03-22_no-code-change-git-remove-added-directory.md`
- 不影响现有功能、构建、测试和 CI 行为。

## 验证结果
- 已检查 `docs/changes/` 目录结构，确认日期子目录规则存在且可扩展。
- 本次无需运行代码测试；文档记录路径符合仓库约定。
- 已知限制：未对用户目标目录执行实际 Git 命令，仅提供通用安全做法。

## 下一步建议
1. 如果目标只是撤销 `git add`，优先使用 `git restore --staged <目录名>`。
2. 如果目标是让 Git 不再跟踪该目录，同时保留本地文件，使用 `git rm -r --cached <目录名>`，必要时再补 `.gitignore`。
