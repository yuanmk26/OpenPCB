## 变更前问题

GUI 文档中只有整体架构和工作台骨架说明，没有单独整理当前原理图预览已经支持的内容、数据流和分层结构，导致后续查看时需要反复读代码确认现状。

## 变更内容

新增 `docs/gui/schematic-preview.md`，系统整理当前 GUI 原理图预览的入口文件、数据流、渲染架构、已支持元素、交互能力、单位体系和已知限制；同时更新 `docs/gui/README.md`，加入原理图预览文档入口并同步当前 scope 描述。

## 影响范围

影响 GUI 文档层，不改动原理图预览运行逻辑、类型结构和交互实现。

## 验证结果

已完成文档内容与当前代码实现的对照检查，确认描述与现有实现一致。

## 下一步建议

建议后续在接入真实原理图数据源时同步更新该文档中的数据流与限制说明，并在 GUI 架构文档中补充预览层与未来编辑层的边界。
