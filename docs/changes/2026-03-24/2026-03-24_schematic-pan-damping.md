## 变更前问题

原理图预览在放大后拖动仍按屏幕位移直接更新平移量，导致页面摆动速度过快，用户难以细致地浏览局部内容。

## 变更内容

为原理图预览的拖动交互增加基于当前缩放比例的阻尼系数，缩放越小、拖动越慢，从而改善放大后浏览局部区域的控制感。

## 影响范围

影响 GUI 原理图预览的拖动手感，不改变页面内容、缩放逻辑和基础渲染数据结构。

## 验证结果

执行 `npm run build`（目录：`src/gui`），构建通过。

## 下一步建议

建议后续将拖动阻尼、缩放步长和缩放边界统一整理为 viewport 交互配置，便于整体调优。
