## 变更前问题

原理图预览只能通过工具栏按钮进行缩放，交互效率较低，不符合画布类视图常见的滚轮缩放使用习惯。

## 变更内容

为原理图预览新增鼠标滚轮缩放交互：滚轮向前放大、向后缩小，并围绕鼠标当前位置进行缩放，同时拦截默认滚动行为。

## 影响范围

影响 GUI 原理图预览的缩放交互方式，不改变页面内容、拖动逻辑和渲染数据结构。

## 验证结果

执行 `npm run build`（目录：`src/gui`），构建通过。

## 下一步建议

建议后续继续统一工具栏缩放和滚轮缩放的步长策略，并视需要增加 `Ctrl + 滚轮` 等快捷交互。
