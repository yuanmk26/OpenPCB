## 变更前问题

原理图预览缺少直观的定位辅助，难以快速判断画布上是否确实渲染了图元，以及当前页到底包含多少可见元素。

## 变更内容

在原理图页面中新增固定的圆形定位标记与十字辅助线，并在状态栏增加当前页的实例、符号图元、导线、标签、连接点和标记数量统计。

## 影响范围

影响 GUI 原理图预览的调试可观察性，不改变原理图几何数据结构、viewport 逻辑和正式图元渲染规则。

## 验证结果

执行 `npm run build`（目录：`src/gui`），构建通过。

## 下一步建议

如果后续还需要更强的诊断能力，建议补充可切换的图元边框高亮和鼠标悬停拾取信息。
