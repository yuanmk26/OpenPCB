## 变更前问题

原理图预览的 mock 页面尺寸偏小，而器件与连线几何尺寸相对偏大，导致整页适配后器件仍显得不符合常见原理图比例。

## 变更内容

放大 mock 原理图页面尺寸，保持器件、连线和标签的相对几何关系不变，以降低器件在页面中的占比。

## 影响范围

影响 GUI 原理图预览示例数据的页面比例表现，不影响 viewport 算法、渲染流程和业务数据结构。

## 验证结果

执行 `npm run build`（目录：`src/gui`），构建通过。

## 下一步建议

后续应将页面尺寸、符号库尺寸和线宽文字大小统一到明确单位体系，避免继续依赖手工经验值。
