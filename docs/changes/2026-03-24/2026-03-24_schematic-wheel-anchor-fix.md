## 变更前问题

原理图预览的滚轮缩放使用基于元素矩形的近似坐标计算焦点，导致缩放锚点与鼠标位置不完全一致，表现为缩放更像围绕页面右下区域进行。

## 变更内容

将原理图预览的滚轮缩放焦点改为基于 SVG 屏幕变换矩阵反算的实际画布坐标，使滚轮缩放严格围绕鼠标当前位置进行。

## 影响范围

影响 GUI 原理图预览滚轮缩放的锚点精度，不改变拖动参数、页面内容和渲染数据结构。

## 验证结果

执行 `npm run build`（目录：`src/gui`），构建通过。

## 下一步建议

建议后续继续统一按钮缩放与滚轮缩放的焦点策略，减少不同交互方式之间的行为差异。
