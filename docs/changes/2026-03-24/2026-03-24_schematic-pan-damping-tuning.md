## 变更前问题

原理图预览在加入拖动阻尼后已有改善，但放大视角下的拖动速度仍偏快，细致浏览局部区域时控制感不足。

## 变更内容

进一步下调原理图预览按缩放比例分段计算的拖动阻尼系数，降低低缩放和中低缩放区间的平移响应速度。

## 影响范围

影响 GUI 原理图预览放大后拖动的手感，不改变页面内容、缩放逻辑和渲染数据结构。

## 验证结果

执行 `npm run build`（目录：`src/gui`），构建通过。

## 下一步建议

如果后续还需继续微调，建议把拖动阻尼改成连续函数或暴露为可配置参数，避免反复调整分段阈值。
