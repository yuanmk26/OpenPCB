## 变更前问题

原理图预览切换到连续拖动阻尼曲线后，整体手感更平滑，但拖动速度仍偏快，局部浏览时控制感不足。

## 变更内容

下调原理图预览连续拖动曲线的最小系数、最大系数和曲线增长速度，使整个缩放区间内的平移响应更加平缓。

## 影响范围

影响 GUI 原理图预览的拖动速度曲线参数，不改变页面内容、缩放逻辑和渲染数据结构。

## 验证结果

执行 `npm run build`（目录：`src/gui`），构建通过。

## 下一步建议

如果后续还需微调，建议继续仅调整拖动曲线常量，不再改动拖动逻辑本身。
