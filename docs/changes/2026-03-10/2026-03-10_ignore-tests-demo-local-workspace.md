# 2026-03-10 忽略 tests/demo 本地测试工作区

## 变更前问题
- `tests/demo` 目录用于本地联调，会产生配置与日志文件。
- 其中部分历史文件曾被 Git 跟踪，后续存在误提交风险。

## 变更内容
- 在 `.gitignore` 中新增忽略规则：`tests/demo/`。
- 将 `tests/demo` 下历史已跟踪文件从 Git 索引移除（保留本地使用场景）。

## 影响范围
- 仅影响版本控制追踪策略，不影响运行逻辑。
- `tests/demo` 目录后续本地产物默认不进入提交。

## 验证结果
- `git status` 显示 `.gitignore` 更新与 `tests/demo` 跟踪移除结果。

